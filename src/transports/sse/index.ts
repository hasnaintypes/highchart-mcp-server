// SSE transport — placeholder
//
// Standalone SSE transport (SSEServerTransport) is deprecated in the MCP SDK.
// StreamableHTTPServerTransport handles SSE fallback natively.
//
// This directory exists as a scaffold in case a dedicated SSE transport
// is needed in the future for backwards-compatibility with older clients.
